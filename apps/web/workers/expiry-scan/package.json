{
	"name": "expiry-scan",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --test-scheduled",
		"start": "wrangler dev --test-scheduled",
		"cf-typegen": "wrangler types",
		"run:expiry-scan": "ts-node-esm --loader ts-node/esm workers/expiry-scan/test.ts"
	},
	"devDependencies": {
		"typescript": "^5.5.2",
		"wrangler": "^4.19.1"
	},
	"dependencies": {
		"dotenv": "^16.5.0"
	}
}
